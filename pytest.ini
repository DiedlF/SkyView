[pytest]
testpaths = tests
markers =
    integration: requires a live Skyview (and optionally Explorer) backend
    perf: performance probes with latency thresholds (slow, opt-in)
addopts = -m "not perf"
